// 레벨별 라우팅 해놓는 파일

// home routes

const HOME = '/';
const LOGIN = '/login';
const JOIN = '/join';
const LOGOUT = '/logout';

const routes = {
	home: HOME,
	login: LOGIN,
	join: JOIN,
	logOut: LOGOUT
};
export default routes;
